# NEON PLAYER X — V8.1 CREATOR GIFT

Actualización acumulativa sobre V8.0.

- Mantiene las funciones anteriores.
- Registra el regalo privado del creador dentro del bolsillo privado de Neon Orb, una sola vez.
- El regalo no se renderiza ni se publica en la interfaz.
- Añade acceso interno `hasCreatorGift()` / `getCreatorGift()` para la lógica de Orb.
- Repara la lectura de estado usada por el canal privado IA.
- Normaliza la lectura de memorias del Orb.
- Refuerza la adaptación responsive para móviles, tabletas y pantallas pequeñas.
- Service Worker actualizado a V8.1 con precache y fallback offline.

El bolsillo del navegador no debe considerarse almacenamiento criptográfico seguro: el cliente puede inspeccionarse mediante herramientas del navegador.
